{
  "name": "adaptive-replacement-cache",
  "version": "1.0.1",
  "description": "A lightweight ARC cache implementation for Node.js and browsers.",
  "homepage": "https://github.com/avoidwork/adaptive-replacement-cache#readme",
  "bugs": {
    "url": "https://github.com/avoidwork/adaptive-replacement-cache/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avoidwork/adaptive-replacement-cache.git"
  },
  "license": "BSD-3-Clause",
  "author": "Jason Mulligan <jason.mulligan@avoidwork.com>",
  "type": "module",
  "main": "dist/arc.cjs",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "build": "npm run rollup",
    "changelog": "auto-changelog -p",
    "coverage": "node --test --experimental-test-coverage --test-coverage-exclude=dist/** --test-coverage-exclude=tests/** --test-reporter=spec tests/**/*.test.js 2>&1 | grep -A 1000 \"start of coverage report\" > coverage.txt && ! grep -q \"failing tests\" coverage.txt",
    "fix": "oxlint --fix *.js benchmarks src tests && oxfmt *.js benchmarks src tests/unit --write",
    "lint": "oxlint *.js benchmarks src tests && oxfmt *.js benchmarks src tests/unit --check",
    "prepare": "husky",
    "rollup": "rollup --config",
    "test": "npm run lint && node --test tests/**/*.test.js",
    "benchmarks": "node benchmarks/benchmarks.js"
  },
  "keywords": [
    "arc",
    "cache",
    "adaptive",
    "replacement",
    "node",
    "browser",
    "typescript",
    "cjs",
    "esm"
  ],
  "types": "types/arc.d.ts",
  "files": [
    "dist/arc.js",
    "dist/arc.cjs",
    "types/arc.d.ts"
  ],
  "exports": {
    "types": "./types/arc.d.ts",
    "import": "./dist/arc.js",
    "require": "./dist/arc.cjs"
  },
  "dependencies": {},
  "devDependencies": {
    "auto-changelog": "^2.5.0",
    "husky": "^9.1.7",
    "oxfmt": "^0.42.0",
    "oxlint": "^1.57.0",
    "rollup": "^4.60.0"
  }
}
